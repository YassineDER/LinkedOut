package org.ichat.backend.model.util.job;

/**
 * JobType is an enum that represents the different types of jobs that can be posted on the platform.
 * @see org.ichat.backend.model.tables.jobs.Job
 */
public enum JobType {
    FULL_TIME,
    PART_TIME,
    FREELANCE,
    INTERNSHIP,
    CUSTOM_CONTRACT
}
